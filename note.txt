2026.7.1
1.今天把最简单的mvp打通，整个的流程从用户的输入开始(input)，然后判断是否为指令性命令，
如果是的话则分别单独处理，否则的话则进行大模型进行分析
2.配置了env里面的link链接以及大模型的链接，用的是minimax
3.大模型的分析处理主要分为三步，第一个为planner，第二个为coder，第三个为runner
4.planner，coder，runner我们对它们分别给不同的prompt。
prompt的构成需要加入身份的认证(告诉大模型他的身份是什么)，
它可以使用哪些工具，它不可以做什么样的事情，它的输出是什么样子的(必须把每一个path都详细的列出来)，
它应该去做什么(详细的看整个代码)，以及如何让它的运行更加的高效。
5.学会了怎么去读取本地的.env文件，然后创建调用Anthropic函数来创建一个简单的agent
resp.stop_reason ∈ {"end_turn", "tool_use", "max_tokens"}这三种的结束方式

2026.7。2
1.完成整个mvp闭环，发现bug。bug产生原因：
    (1)coder这边没拦截，直接将planner这边的输出当做计划了，需要加强prompt隔离。
    (2)coder修改了不该修改的文件，需要加workdir的限制。
2.重构整个框架

2026.7.5
1.完成schema校验。一方面在prompt里面明确，planner的输出格式，另一方面用json库来提纯，清理，兜底planner的输出格式。同时明确了白名单。
2.coder的话也是，一方面prompt明确了coder修改只能在指定的作用于内，同时对write file这个工具而言，必须在白名单内操作，否则则报错退出。
3.完成了memory系统，但是暂未检验。
首先是创建了SessionTurn保存每一轮的记录，然后要做的是把之前的进行压缩，保留Head(第一轮SessionTurn)，tail(最后三轮List[SessionTurn]),
middle(List[str],每一个都进行了压缩).然后把compact_history给全部搞成三个str的拼接，就是context历史记录了。作为user message前面的拼接。
最后就是把新的议论的StageResult给处理一下，Planer取artifact里面的summary，coder取artifact，runner不管先。存到这个SessionTurn。
4.定位问题，解决一个schema校验太强的问题。