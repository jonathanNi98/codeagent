# LLM SDK — we use the Anthropic SDK because MiniMax exposes an
# Anthropic-compatible endpoint at https://api.minimaxi.com/anthropic.
anthropic>=0.30.0

# .env loader
python-dotenv>=1.0.0

# Terminal rendering
rich>=13.0.0
