# LLM SDK — pick whichever matches your MiniMax endpoint.
# OpenAI-compatible (default expectation):
openai>=1.0.0
# Anthropic-compatible (uncomment if your provider is Anthropic-style):
# anthropic>=0.30.0

# .env loader
python-dotenv>=1.0.0

# Terminal rendering
rich>=13.0.0
